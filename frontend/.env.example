# Frontend env example
# API base URL used by the frontend (change if backend runs on another host/port)
VITE_API_URL=http://localhost:8000
# Splash duration in milliseconds
VITE_SPLASH_DURATION=1000
